# Copy this file to `.env.local` (ignored by git) and populate with your secrets.
OPENAI_API_KEY=sk-your-openai-key
# Optional overrides:
# OPENAI_BASE_URL=https://api.openai.com
# OPENAI_MODEL=gpt-4o-mini

# Default local analysis proxy (mirrors npm run dev:server)
UXBIBLIO_ANALYSIS_URL=http://localhost:4292

# Set to forward analysis requests to a local/staging UXBiblio instance that enforces CSRF.
# When provided, the proxy rewrites browser-originated requests through Node to bypass Origin blockers.
# UXBIBLIO_ANALYSIS_UPSTREAM_URL=http://localhost:4111
